# 11 — Provenance & Recency Flags  ·  [BASE]

> **Status:** SEEDED POINTER — to be operationalized by Fable (master-prompt Commission J).
> **Purpose.** Track what still needs a source, and where newer scholarship could correct or
> strengthen a beat. Flags only — resolutions land as `fact:` commits.
> Structure provisional — reshape freely; log changes in `12-decision-log.md`.

## The live soft-spot list
The authoritative list of items resting on Jake's transcriptions or single tellings — each
needing a page-number note by commit — lives in **`PROJECT_NOTES.md` §3** ("Soft spots — confirm
during read-throughs"). Do not duplicate it here; operationalize it: one row per item with
status, source-to-check, and outcome.

## What belongs here
- A working table: `item | rests on | source to check | status | [VERIFY]/resolved`.
- The read-through order that will resolve them (PROJECT_NOTES §5: Erskine → Reynolds journal →
  Wilkes → Stanton/Philbrick to arbitrate).
- **Recency flags:** where post-publication scholarship on the Ex. Ex. may have moved; each with
  what the book currently says, what may have changed, confidence, and `[VERIFY]`. Be explicit
  about training-cutoff limits; never assert an update you can't verify.

## Conventions
Flags only; this file never changes the base. Resolutions become `fact:` commits citing the
source. [VERIFY] everything unconfirmed.

---
*(seeded — awaiting the Fable pass to build the table)*
